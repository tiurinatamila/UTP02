// TODO: we need to add the missing classes!

// OK, I will add `Adder` and s30829 will add `Subtractor`.

public class Main {
    public static void main(String[] args) {
        Adder adder = new Adder();
        System.out.println(adder.add(11, 4));
        Subtractor subtractor = new Subtractor();
        System.out.println(subtractor.subtract(10, 2));
    }
}
